---
import BaseHead from "@components/section/BaseHead.astro";

interface LayoutProps {
  title: string;
  description: string;
}
const url = Astro.url.pathname;

const { title, description }: LayoutProps = Astro.props as LayoutProps;
---

<!doctype html>
<html lang="en">
  <head>
    <BaseHead title={title} description={description} />
  </head>
  <body>
    <!-- <Header /> -->
    <slot />
    <!-- <Footer /> -->
  </body>
</html>
